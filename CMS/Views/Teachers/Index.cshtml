@model IEnumerable<CMS.Models.Teacher>

@{
    ViewBag.Title = "Index";
}

<h2>Teachers List</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Teacher_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Teacher_DOB)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Teacher_Status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Teacher_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Teacher_DOB)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Teacher_Status)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Teacher_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Teacher_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Teacher_Id })
        </td>
    </tr>
}

</table>
